여기는 새로운 branch입니다.


git status 
:commit 안된 내용있는지 확인 가능 (붉은색파일)

commit 하는 방법

git add -A
git commit -m "코멘트 내용"

브랜치 만드는 방법
git branch branch이름

브랜치 이동
git checkout branch이름

브랜치 합치기
git checkout master 마스터 브랜치로 이동
get merge 합치고 싶은 branch이름

브래치 흐름도 보기
git log --graph --all --decorate

.gitignore -> 깃에 안올릴파일 파일을 만들어야함
파일안에 올리면 안되는 보안 파일의 이름을 적어놓으면 안올라감 status입력시 목록에 나오지 않음




github에 등록
git push origin main